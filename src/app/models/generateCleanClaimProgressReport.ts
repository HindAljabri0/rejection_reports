export class generateCleanClaimProgressReport {
    payerId = '';
    beforeDate = '';
    afterDate = '';
    comparisionCriteria = 'Year';
    comparisonType = '';
}

export enum GrowthRate {
    Up = 1,
    Down = 2,
    Equal = 0
}