import { ImageToolTipDirective } from './image-tool-tip.directive';

describe('ImageToolTipDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageToolTipDirective();
    expect(directive).toBeTruthy();
  });
});
